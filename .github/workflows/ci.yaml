name: ci

on: push

jobs:
  lint-gql:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - uses: Updater/action-graphql-linter@tmp/test-github-action
        with:
          files: 'schema.graphql'
          # combined: 'false'